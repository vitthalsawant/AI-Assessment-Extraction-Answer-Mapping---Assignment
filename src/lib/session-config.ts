/** How long the client keeps polling while extraction is in progress. */
export const PROCESSING_MAX_WAIT_MS = 20 * 60 * 1000;

/** Delay between session status polls. */
export const SESSION_POLL_INTERVAL_MS = 2000;
